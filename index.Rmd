---
title: "Austin Animal Shelter Analysis"
author: "Anthony Ngo"
date: "July 28, 2019"
output: html_document
---

```{r , include=FALSE}
library(knitr)
library(ggplot2)
library(dplyr)
source("scripts/summary_info.R")

in_df <- read.csv("data/aac_intakes.csv", stringsAsFactors = F)
out_df <- read.csv("data/aac_outcomes.csv", stringsAsFactors = F)
in_out_df <- read.csv("data/aac_intakes_outcomes.csv", stringsAsFactors = F)

out_info <- summary_info_out(out_df)
in_info <- summary_info_in(in_df)
in_out_info <- summary_info_in_out(in_out_df)
```

# Preiminary Information

## Why We're Looking at the Austin Animal Shelter Data

The Austin Animal Shelter is the largest no-kill animal shelter in the United States

## Where we got our data

Kaggle

## What we want to learn about the Austin Animal Shelter

# Analysis
## Summary Analysis
## Grouped Analysis
# Visualizations

